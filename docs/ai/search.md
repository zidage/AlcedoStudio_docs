---
sidebar_position: 4
---

# AI 搜索与模糊搜索

Alcedo Studio 提供全局搜索，支持三种检索方式：模糊匹配（按元数据与标签检索）、语义搜索（用自然语言描述检索）、以及精确匹配（直接定位某张图像）。点击左侧“Search”标签打开搜索对话框，搜索框占位符为“Search photos, cameras, lenses, dates...”。

## 模糊搜索

在搜索框中输入关键词（相机型号、镜头、拍摄日期、标签等），输入时下方会实时出现结果预览，顶部显示匹配数（如“1-24 of 87 matches”）。每条结果包含缩略图、文件名、相机型号与拍摄日期，以及评分或镜头信息。

按回车或点击“Search”按钮，会将当前查询作为模糊筛选应用到相册，对话框关闭，右侧检查器面板出现“SEARCH FILTER”卡片显示当前查询，可点击其“x”清除。也可以直接点击结果行，以精确匹配方式定位到该图像。

未输入内容时，对话框显示“Suggestion”列表，按相机、日期、镜头等分类列出可用的搜索建议，点击某项即可作为模糊查询应用。结果较多时，列表底部会出现“Load more”、向上滚动到顶部附近会出现“Load previous”，用于翻页加载。

## 语义搜索

打开对话框右上方的“Semantic”开关后，可使用语义搜索。语义搜索通过本地的 CLIP 模型，把你输入的自然语言描述与图像内容进行比对，返回内容上最相关的图像。例如输入“a dog on the beach”或“夜景人像”之类描述，即可找到内容相符的图像，而不必依赖文件名或元数据中是否恰好包含这些词。

语义搜索的运行方式与模糊搜索略有不同：

- 输入时仍会实时预览，但语义搜索需要按回车或点击“Search”才会正式提交，预览阶段可能会提示“Press Enter or click Search for semantic search”。
- 查询过长时会提示“Query is too long for semantic search”，需要缩短后重试。
- 若未激活模型，会提示“Semantic search is not available yet”，此时需先在设置中下载并激活模型，详见 [AI 模型管理](./model-management.md)。

是否走语义路径由软件自动判定：开启 Semantic 开关后，符合语义条件的查询走语义路径，其余查询仍按模糊匹配处理。

## 精确匹配

点击任意一条搜索结果，会以精确匹配方式直接定位到该图像（相当于按图像身份筛选），对话框随即关闭。

## 三种方式的区别

- **模糊搜索**：按已有的元数据与标签做关键词匹配，速度快，适合知道相机、镜头、日期或标签等明确信息时使用。
- **语义搜索**：按图像内容做自然语言检索，需要模型已激活，适合按画面内容而非文件信息查找时使用。
- **精确匹配**：从搜索结果中直接选中某一张图像。

未激活模型时，语义搜索不可用，但模糊搜索与精确匹配仍可正常使用。